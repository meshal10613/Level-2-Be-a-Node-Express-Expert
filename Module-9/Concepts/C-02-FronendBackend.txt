1. Frontend • 2. Backend • 3. Node.js 4.Server-Side Rendering 5.Client-Side Rendering 6.Hybrid Rendering  7.Server 8.DATABASE 9.MASTER–SLAVE DATABASE SYSTEM


🟦 Frontend কাকে বলে?

ফ্রন্টএন্ড হল ওয়েবসাইট বা অ্যাপের সেই অংশ যেটা ইউজার সরাসরি দেখে এবং ব্যবহার করে।

যেমন — ডিজাইন, বাটন, ছবি, টেক্সট, ফরম, মেনু — সবই Frontend।

✔️ সহজ উদাহরণ:

Facebook খুললে যেগুলো দেখেন—

Login Form

News Feed

Like Button

Photos

Comments Section

সবই Frontend।

✔️ Frontend কোন ভাষা/টেক দিয়ে তৈরি হয়?

মূল ৩টি ভাষা:

HTML → কাঠামো

CSS → ডিজাইন

JavaScript → লজিক/ইন্টারঅ্যাকশন

জনপ্রিয় ফ্রন্টএন্ড ফ্রেমওয়ার্ক/লাইব্রেরি:

React

Vue

Angular

Svelte

🟩 Backend কাকে বলে?

ব্যাকএন্ড হল সার্ভারের ভিতরের লুকানো অংশ যেখানে—
ডেটা প্রসেসিং, অ্যাপ লজিক, সিকিউরিটি, ডেটাবেস ম্যানেজমেন্ট ইত্যাদি কাজ হয়।

ইউজার এটা দেখে না, কিন্তু পুরো সিস্টেম চালানোর দায়িত্ব ব্যাকএন্ডের।

✔️ সহজ উদাহরণ:

Facebook-এ “Login” চাপলে—

আপনার ইমেইল/পাসওয়ার্ড সার্ভারে যায়

সার্ভার চেক করে সঠিক কি না

সঠিক হলে Homepage দেখায়

এগুলো সবই Backend এর কাজ।

✔️ Backend কোন ভাষা/টেক দিয়ে তৈরি হয়?

প্রধান প্রোগ্রামিং ভাষা:

Node.js (JavaScript/TypeScript)

Python (Django, Flask, FastAPI)

PHP (Laravel)

Java (Spring Boot)

C# (.NET)

Go (Golang)

Ruby (Rails)

ডেটাবেস:

MySQL

PostgreSQL

MongoDB

SQLite

Oracle
⁡⁣⁢⁢
//********************************************!SECTION⁡

Node.js কেন তৈরি করা হয়েছিল?

✔️ ১. সার্ভারকে দ্রুত ও স্কেলেবল করার জন্য

আগে PHP, Python, Ruby ইত্যাদি সার্ভার একসাথে অনেক রিকোয়েস্ট ঠিকভাবে হ্যান্ডেল করতে পারত না। ফলে সার্ভার স্লো হয়ে যেত।

Node.js এ সমস্যা ছিল না, কারণ—

এটি non-blocking

এটি asynchronous

এক থ্রেড দিয়েই অনেক রিকোয়েস্ট হ্যান্ডেল করতে পারে

মানে:
একটি Node.js সার্ভার একই সময়ে হাজার হাজার ইউজারের রিকোয়েস্টকে দ্রুত উত্তর দিতে পারে।

✔️ ২. সার্ভারে JavaScript চালানোর জন্য

Node.js হওয়ার আগে JavaScript শুধু ব্রাউজারেই চলত।
Node.js বানানো হয়েছে যেন:

ফ্রন্টএন্ড → JavaScript

ব্যাকএন্ড → JavaScript

অর্থাৎ একই ভাষা দিয়ে পুরো অ্যাপ্লিকেশন তৈরি করা যায়।
========================================
🟦 1) Server-Side Rendering (SSR) – কাকে বলে?
========================================

✔️ সহজ সংজ্ঞা:
SSR মানে—ওয়েবপেজের HTML ব্রাউজারে নয়, সার্ভারে তৈরি হয়।  
ইউজার পেজ খুললে সার্ভার আগে সম্পূর্ণ HTML বানিয়ে ব্রাউজারে পাঠায়।

✔️ কীভাবে কাজ করে?
User Request  
→ Server HTML তৈরি করে  
→ Browser সেই HTML সরাসরি দেখায়  
→ তাই প্রথমবার পেজ দ্রুত লোড হয় (Fast First Load)


========================================
🟦 2) Client-Side Rendering (CSR) – কাকে বলে?
========================================

👉 সংজ্ঞা:
CSR-এ ওয়েবসাইট প্রথমে প্রায় খালি পেজ লোড করে।  
তারপর JavaScript ডাউনলোড হয়ে ব্রাউজারের ভিতর পেজ তৈরি করে।

🟩 আরও সহজ উদাহরণ (রেস্টুরেন্ট উদাহরণ):

CSR হলো:
রেস্টুরেন্ট তোমাকে কাঁচা উপকরণ দিল →  
তুমি টেবিলে বসে রান্না করলে →  
তাই খাবার পেতে সময় লাগে।

ওয়েবসাইটও:
Empty Page → JavaScript লোড → JS পেজ তৈরি → UI দেখা


========================================
🟦 3) Hybrid Rendering – কী?
========================================

Hybrid Rendering = SSR + CSR দুইটাই ব্যবহার।

মানে:
একই ওয়েব অ্যাপে কিছু পেজ সার্ভার রেন্ডার করবে (SSR),  
আর কিছু পেজ ব্রাউজার নিজে রেন্ডার করবে (CSR)।

এটা আধুনিক ফ্রেমওয়ার্কগুলোর সবচেয়ে বড় সুবিধা:
- Next.js
- Nuxt.js

========================================

⁡⁣⁣⁢//********************************************!SECTION
⁡ব্রাউজার থেকে সার্ভার পর্যন্ত একটি রিকোয়েস্ট কীভাবে যাত্রা করে এবং মাঝে কী কী ধাপ ঘটে
=== Browser থেকে Server পর্যন্ত Request Flow (সহজ উদাহরণ) ===

1) তুমি বন্ধুকে ফোন দাও
   → ব্রাউজার কোনো ওয়েবসাইটে যাওয়ার জন্য রিকোয়েস্ট পাঠায়।

2) কিন্তু তুমি বন্ধুর নাম জানো, নম্বর জানো না
   → ব্রাউজার DNS-কে জিজ্ঞেস করে: “facebook.com এর ঠিকানা (IP) দাও।”

3) DNS ফোনবুকের মতো
   → DNS খুঁজে বের করে প্রকৃত নম্বর (IP Address)।

4) DNS নম্বর দিয়ে দেয়
   → ব্রাউজার এখন জানে সার্ভার কোথায় আছে।

5) তুমি সেই নম্বরে কল করো
   → ব্রাউজার সার্ভারের কাছে রিকোয়েস্ট পাঠায়।

6) বন্ধু তোমার কথা শোনে
   → সার্ভার তোমার রিকোয়েস্ট পড়ে, কী চেয়েছো বুঝে।

7) সে উত্তর দেয়
   → সার্ভার ব্রাউজারকে ডাটা/HTML/JSON পাঠায়।

8) তুমি উত্তর শুনে বুঝো
   → ব্রাউজার সেই ডাটা নিয়ে UI দেখায়—ওয়েবসাইট স্ক্রিনে রেন্ডার হয়।

সারসংক্ষেপ:
Browser Request → DNS Lookup → IP Found → Server Request → Server Process → Response → Browser Render



..

==============================
SERVER কী? (সহজ ব্যাখ্যা)
==============================

Server হলো এমন একটি কম্পিউটার/সিস্টেম যেটা ব্রাউজারের Request গ্রহণ করে, প্রসেস করে এবং Response পাঠায়।

তুমি ব্রাউজারে facebook.com লিখলে → Request যায় Server এর কাছে।
Server ডাটাবেস থেকে ডাটা নেয়, লজিক চালায়, তারপর তোমার কাছে রেজাল্ট পাঠায়।


==============================
SERVER এর দরকারি কিছু জিনিস
==============================

1) Load Balancer  
   → ট্রাফিক অনেক হলে, Load Balancer দেখে কোন server-এ চাপ কম।  
   → তারপর Request সেই কম-ব্যস্ত server-এর দিকে পাঠায়।  
   → এতে সার্ভার ডাউন হওয়ার ঝুঁকি কমে।

   সহজ উদাহরণ:  
   দোকানে ভিড় হলে একজন ম্যানেজার লোকজনকে আলাদা আলাদা লাইনে পাঠিয়ে দেয় —
   ঠিক সেভাবে Load Balancer user-দের ভিন্ন server-এ পাঠায়।


==============================
SERVER SCALING
==============================

স্কেলিং মানে বেশি লোড সামলানোর ক্ষমতা বাড়ানো।

► Vertical Scaling  
   → একই server-এ RAM, CPU বাড়িয়ে শক্তিশালী করা।  
   → একটাকে "মোটাসোটা" করা।

► Horizontal Scaling  
   → নতুন server যোগ করা (server 1, server 2, server 3...)  
   → Load Balancer এগুলোর মধ্যে ট্রাফিক ভাগ করে দেয়।  
   → মানে "আরও মানুষ যুক্ত করা"।

উদাহরণ:  
একটা দোকানে ভিড় হলে  
Vertical = দোকানটা বড় করা  
Horizontal = আরও দোকান খোলা


==============================
DATABASE এর কাজ কী?
==============================

Database হচ্ছে এমন জায়গা যেখানে ডাটা **সংরক্ষণ, পড়া, আপডেট, ডিলিট** করা যায়।

দুই ধরনের কাজ হয়:
1) Write Operation → নতুন ডাটা লেখা / পরিবর্তন করা  
2) Read Operation  → ডাটা পড়া


==============================
MASTER–SLAVE DATABASE SYSTEM
==============================

1) Master DB  
   → এখানে Write কাজ হয়  
   → যেমন: নতুন ইউজার রেজিস্ট্রেশন, পোস্ট তৈরি, পাসওয়ার্ড আপডেট  

2) Slave DB  
   → এখানে Read কাজ হয়  
   → যেমন: নিউজফিড দেখা, প্রোফাইল দেখা, কমেন্ট লোড হওয়া  

এটা করা হয় লোড কমানোর জন্য।  
কারণ WRITE কাজ বেশি ভারী, আর READ কাজ অনেক বেশি হয়।


==============================
যদি Master DB নষ্ট হয়ে যায়?
==============================

► তখন একটি Slave DB, Master DB-এর জায়গায় Master হয়ে যায়।  
► একে বলে Failover system।  
► ফলে পুরো সিস্টেম বন্ধ হয়ে যায় না।


==============================
সংক্ষেপে পুরো সিস্টেম flow
==============================

Browser Request  
→ Load Balancer  
→ সঠিক Server নির্বাচন  
→ Server Logic চলছে  
→ Database (Master = Write, Slave = Read)  
→ Result  
→ Response ব্রাউজারে ফিরে আসে  
→ UI Render হয়

==============================



 